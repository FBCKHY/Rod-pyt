， Art Design Pro 后台管理系统启动说明

## 📋 概述

本项目提供了两套完整的启动脚本：**内部版本**（适合Cursor内部终端）和**外部版本**（适合独立终端窗口），每套都包含完整的菜单和功能脚本。

## ⚠️ **重要说明：版本区别**

### 🔑 **核心区别**
- **内部版本** ✅ = **直接在当前Cursor终端中运行，不打开新窗口**
- **外部版本** = **打开独立的外部PowerShell窗口**

### 🎯 **使用场景**
- **开发环境**: 使用内部版本，在Cursor内部终端操作
- **独立使用**: 使用外部版本，双击直接运行

### 🚀 **服务端口**
- **后台管理系统**: http://localhost:3006/ (admin/)
- **后端API服务**: http://localhost:3001/api/ (backend/) 🆕

### 💡 **多服务启动说明**
要同时运行后台管理和后端API，您需要：
1. **内部版本**: 在Cursor中打开两个终端标签，分别运行
2. **外部版本**: 双击两个不同的bat文件，会打开两个独立窗口

## 🎯 脚本分类

### 内部版本 (`内部/` 目录)
- **适用场景**: 在Cursor或VS Code等IDE内部终端使用
- **运行方式**: **直接在当前Cursor终端中运行，不打开新窗口**
- **特点**: 输出简洁，快速返回，无过多等待
- **界面**: 简洁明了，适合开发环境
- **重要**: 无论如何启动，都在Cursor内部终端执行

### 外部版本 (`外部/` 目录)  
- **适用场景**: 独立终端窗口，双击直接运行
- **运行方式**: **打开独立的外部PowerShell窗口**
- **特点**: 详细输出，完整提示，用户友好
- **界面**: 美观完整，适合独立使用
- **重要**: 始终在新的外部窗口中运行

## 📂 文件结构

```
启动服务器/
├── 内部/                          # Cursor内部终端版本
│   ├── 菜单.bat                   # 主菜单：[1]后台管理 [2]后端API [3]停止 [4]检查 [5]退出
│   ├── 快速启动后台.bat           # 快速启动后台管理（跳过检查）
│   ├── 快速启动后端.bat           # 🆕 快速启动后端API（跳过检查）
│   └── 其他启动/
│       ├── 启动后台管理系统.bat   # 完整启动后台管理（含检查）
│       ├── 启动后端API服务.bat    # 🆕 完整启动后端API（含检查）
│       ├── 停止服务器.bat         # 停止所有服务器
│       ├── 检查配置依赖.bat       # 检查配置依赖
│       └── 创建后端目录.bat       # 创建后端项目目录
├── 外部/                          # 独立终端版本
│   ├── 菜单.bat                   # 主菜单：[1]后台管理 [2]后端API [3]停止 [4]检查 [5]退出
│   ├── 快速启动后台.bat           # 快速启动后台管理（跳过检查）
│   ├── 快速启动后端.bat           # 🆕 快速启动后端API（跳过检查）
│   └── 其他启动/
│       ├── 启动后台管理系统.bat   # 完整启动后台管理（含检查）
│       ├── 启动后端API服务.bat    # 🆕 完整启动后端API（含检查）
│       ├── 停止服务器.bat         # 停止所有服务器
│       └── 检查配置依赖.bat       # 检查配置依赖
└── 启动说明.md                    # 本说明文档
```

## 🚀 使用方法

### 📱 内部版本使用（推荐在Cursor中使用）

#### 1. 菜单方式（推荐）
```bash
# 在Cursor内部终端中运行
cd "网站工具/启动服务器/内部"
./菜单.bat
```

菜单选项：
- `[1]` 启动后台管理系统（完整检查）
- `[2]` 启动后端API服务（完整检查）🆕
- `[3]` 停止所有服务器
- `[4]` 检查配置依赖
- `[5]` 退出

#### 2. 直接运行
```bash
# 后台管理系统
./内部/快速启动后台.bat              # 快速启动（日常使用）

# 后端API服务
./内部/快速启动后端.bat              # 🆕 快速启动后端API

# 完整启动（首次使用推荐）
./内部/其他启动/启动后台管理系统.bat    # 后台管理（含检查）
./内部/其他启动/启动后端API服务.bat     # 🆕 后端API（含检查）

# 服务管理
./内部/其他启动/停止服务器.bat          # 停止所有服务器
./内部/其他启动/检查配置依赖.bat        # 检查配置
```

### 🖥️ 外部版本使用（独立运行）

#### 1. 菜单方式（推荐）
双击 `外部/菜单.bat` 打开主菜单

菜单功能：
- 完整的图形界面
- 详细的操作提示
- 错误处理和建议
- 操作完成确认

#### 2. 直接运行
双击对应的bat文件：

**快速启动（日常使用）**
- `外部/快速启动后台.bat` - 快速启动后台管理
- `外部/快速启动后端.bat` - 🆕 快速启动后端API

**完整启动（首次使用推荐）**
- `外部/其他启动/启动后台管理系统.bat` - 完整启动后台管理
- `外部/其他启动/启动后端API服务.bat` - 🆕 完整启动后端API

**服务管理**
- `外部/其他启动/停止服务器.bat` - 停止所有服务器
- `外部/其他启动/检查配置依赖.bat` - 检查配置

## ⚙️ 功能详解

### 🔧 启动后台管理系统
**完整的环境检查和启动流程**

**检查项目**：
- ✅ Node.js环境（要求≥18.20.4）
- ✅ pnpm包管理器（自动安装）
- ✅ 项目依赖（自动安装）
- ✅ 环境配置文件
- ✅ 端口占用情况（3006）

**启动信息**：
- 🌐 本地访问：http://localhost:3006/
- 🌐 网络访问：http://[您的IP]:3006/
- 🛠️ Vue DevTools：http://localhost:3006/__devtools__/

### ⚡ 快速启动后台
**跳过检查，直接启动后台管理系统**

**适用场景**：
- 环境已配置完成
- 日常开发使用
- 快速重启服务器

### 🚀 后端API服务启动 🆕
**完整的后端全栈服务启动功能**

**快速启动后端API**：
- 基本数据库服务检查
- 自动启动Redis/Memurai服务
- 自动创建.env配置文件
- 适用于日常开发和API测试

**完整启动后端API**：
- 环境检查：Node.js、pnpm、依赖包
- 数据库服务检查：PostgreSQL、Redis状态
- 配置验证：环境变量、数据库连接
- Prisma客户端生成和数据库初始化
- 自动安装缺失的依赖
- 适用于首次启动或环境变更后

**数据库服务管理**：
- PostgreSQL：自动检查服务状态和端口监听
- Redis/Memurai：自动检查并启动服务
- 数据库连接：自动验证和故障提示
- Prisma ORM：自动生成客户端

**服务端口分配**：
- 后台管理系统：`http://localhost:3006/`
- 后端API服务：`http://localhost:3001/api/`
- API健康检查：`http://localhost:3001/api/health`
- PostgreSQL数据库：`localhost:5432`
- Redis缓存：`localhost:6379`

### 🛑 停止服务器
**智能停止所有全栈服务进程**

**功能特点**：
- 自动查找占用3006端口的进程（前端管理系统）
- 自动查找占用3001端口的进程（后端API服务）
- 强制终止相关应用进程
- 验证端口释放状态
- 显示详细操作结果和状态确认

**注意事项**：
- 不会停止PostgreSQL和Redis数据库服务
- 只停止应用层服务，保留数据库连接
- 可安全重启应用而无需重启数据库

### 🔍 检查配置依赖
**全面的全栈系统诊断工具**

**检查项目**：
- 📁 目录结构检查（admin/ 和 backend/）
- 🌍 环境变量检查（Node.js、pnpm）
- 🗄️ 数据库服务检查（PostgreSQL、Redis状态）
- 📦 依赖包检查（前端和后端 node_modules）
- ⚙️ 配置文件检查（前后端 .env 文件）
- 🔌 端口占用检查（3006、3001、5432、6379）
- 🗄️ 数据库配置检查（Prisma schema）
- 🔧 Prisma客户端状态检查

## 🎨 界面区别

### 内部版本界面特点
```
[检查] 正在检查环境...
[成功] Node.js: v22.17.1
[成功] pnpm: 10.13.1
[启动] 启动地址: http://localhost:3006/
```

### 外部版本界面特点
```
╔══════════════════════════════════════╗
║        Art Design Pro 后台管理       ║
║            完整启动器               ║
╚══════════════════════════════════════╝

[信息] Admin目录: C:\path\to\admin
[成功] Node.js已安装: v22.17.1
[成功] pnpm已安装: 10.13.1

╔══════════════════════════════════════╗
║               启动信息               ║
╚══════════════════════════════════════╝
```

## 🔧 环境要求

### 必需环境
- **Node.js**: 18.20.4 及以上版本
- **操作系统**: Windows 10/11
- **PowerShell**: 支持UTF-8编码

### 自动安装
- **pnpm**: 脚本会自动检查并安装
- **项目依赖**: 首次运行自动安装

## 🎯 使用建议

### 💡 最佳实践

1. **首次使用**
   - 使用"启动后台管理系统"进行完整检查
   - 建议先运行"检查配置依赖"诊断问题

2. **日常开发**
   - 使用"快速启动后台"提高效率
   - 在Cursor中使用内部版本

3. **问题排查**
   - 先运行"停止服务器"释放端口
   - 使用"检查配置依赖"诊断问题
   - 查看详细错误信息

### 🔄 推荐工作流

#### Cursor开发者工作流
```bash
# 方式1: 菜单启动（推荐）
1. 打开Cursor
2. 在终端中运行: ./网站工具/启动服务器/内部/菜单.bat
3. 选择[1]启动后台管理 或 选择[2]启动后端API
4. 开始开发工作
5. 按Ctrl+C停止或选择[3]停止服务器

# 方式2: 直接启动（快速）
1. 第一个终端: ./网站工具/启动服务器/内部/快速启动后台.bat
2. 第二个终端: ./网站工具/启动服务器/内部/快速启动后端.bat
3. 开始全栈开发
```

#### 独立使用工作流
```bash
1. 双击: 外部/菜单.bat
2. 首次使用选择[1]后台管理完整启动 或 [2]后端API完整启动
3. 日常使用双击: 外部/快速启动后台.bat 或 外部/快速启动后端.bat
4. 需要时选择[3]停止所有服务器
```

## 🐛 故障排除

### 常见问题及解决方案

#### 1. Node.js未安装
**错误提示**: `Node.js未安装或未添加到PATH中`
**解决方案**: 
- 访问 https://nodejs.org/ 下载安装
- 确保添加到系统PATH

#### 2. 端口被占用
**错误提示**: `端口3006已被占用`
**解决方案**:
- 运行"停止服务器"脚本
- 或修改`admin/.env`中的端口配置

#### 3. 依赖安装失败
**错误提示**: `依赖安装失败`
**解决方案**:
- 检查网络连接
- 尝试手动运行：`pnpm install --ignore-scripts`
- 删除`node_modules`文件夹后重新安装

#### 4. 权限问题
**错误提示**: `拒绝访问`
**解决方案**:
- 以管理员身份运行脚本
- 检查防火墙设置

### 🔧 手动操作

如果脚本无法正常工作，可以手动执行：

```bash
# 后台管理系统
cd admin
pnpm install
pnpm dev

# 后端API服务 🆕
cd backend
pnpm install
pnpm db:generate    # 生成Prisma客户端
pnpm run dev

# 停止服务器：按Ctrl+C
```

## 🛠️ 启动方式详解

### 📋 手动启动方法

#### 方法一：命令行手动启动
```bash
# 1. 打开PowerShell或命令提示符
# 2. 导航到项目目录
cd "C:\Users\用户名\Desktop\oai 0\admin"

# 3. 检查环境（可选）
node --version
pnpm --version

# 4. 安装依赖（首次运行或依赖更新时）
pnpm install

# 5. 启动开发服务器
pnpm dev

# 6. 停止服务器
# 按 Ctrl+C 组合键
```

#### 方法二：分步手动验证启动
```bash
# 第一步：环境检查
echo "检查Node.js环境..."
node --version
if ($LASTEXITCODE -ne 0) { 
    Write-Host "Node.js未安装！" -ForegroundColor Red
    exit 1
}

# 第二步：检查pnpm
echo "检查pnpm..."
pnpm --version
if ($LASTEXITCODE -ne 0) {
    Write-Host "正在安装pnpm..." -ForegroundColor Yellow
    npm install -g pnpm
}

# 第三步：进入项目目录
cd admin
if (-not (Test-Path "package.json")) {
    Write-Host "未找到package.json文件！" -ForegroundColor Red
    exit 1
}

# 第四步：检查依赖
if (-not (Test-Path "node_modules")) {
    Write-Host "正在安装依赖..." -ForegroundColor Yellow
    pnpm install
}

# 第五步：启动服务器
Write-Host "启动服务器..." -ForegroundColor Green
pnpm dev
```

#### 方法三：使用npm替代pnpm（备用方案）
```bash
# 如果pnpm有问题，可以使用npm
cd admin

# 使用npm安装依赖
npm install

# 使用npm启动
npm run dev

# 或使用yarn
yarn install
yarn dev
```

### 🤖 自动启动方法

#### 方法一：Windows任务计划程序自动启动

**创建自动启动任务：**

1. **打开任务计划程序**
   ```bash
   # 按Win+R，输入taskschd.msc，回车
   ```

2. **创建基本任务**
   - 任务名称：`Art Design Pro 后台自启动`
   - 描述：`开机自动启动Art Design Pro后台管理系统`

3. **触发器设置**
   - 选择：`当计算机启动时`
   - 延迟：`30秒`（等待系统完全启动）

4. **操作设置**
   - 程序/脚本：`C:\Windows\System32\cmd.exe`
   - 参数：`/c "cd /d "项目路径\网站工具\启动服务器\外部" && 快速启动后台.bat"`
   - 起始于：`项目路径\网站工具\启动服务器\外部`

5. **条件设置**
   - ✅ 只有在计算机使用交流电源时才启动任务
   - ✅ 如果计算机开始使用电池电源，则停止

#### 方法二：创建自动启动脚本

**创建自启动脚本文件：**

```batch
@echo off
chcp 65001 >nul
title Art Design Pro 自动启动服务

:: 设置项目路径（请修改为您的实际路径）
set "PROJECT_PATH=C:\Users\%USERNAME%\Desktop\oai 0"
set "ADMIN_PATH=%PROJECT_PATH%\admin"

:: 等待系统完全启动
timeout /t 30 /nobreak >nul

:: 检查项目目录
if not exist "%ADMIN_PATH%" (
    echo [错误] 项目目录不存在: %ADMIN_PATH%
    pause
    exit /b 1
)

:: 切换到项目目录
cd /d "%ADMIN_PATH%"

:: 启动服务器
echo [自启动] 正在启动Art Design Pro后台管理系统...
pnpm dev

:: 如果启动失败，暂停显示错误
if errorlevel 1 (
    echo [错误] 启动失败！
    pause
)
```

**设置自启动：**

1. **方式一：启动文件夹**
   ```bash
   # 1. 按Win+R，输入shell:startup，回车
   # 2. 将上述脚本保存为"ArtDesignPro自启动.bat"
   # 3. 复制到打开的启动文件夹中
   ```

2. **方式二：注册表设置**
   ```registry
   # 创建注册表项（需要管理员权限）
   [HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Run]
   "ArtDesignPro"="C:\\路径\\到\\自启动脚本.bat"
   ```

#### 方法三：使用PM2进程管理器（推荐）

**安装PM2：**
```bash
# 全局安装PM2
npm install -g pm2

# 或使用pnpm
pnpm add -g pm2
```

**创建PM2配置文件 `ecosystem.config.js`：**
```javascript
module.exports = {
  apps: [{
    name: 'art-design-pro',
    script: 'pnpm',
    args: 'dev',
    cwd: './admin',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'development',
      PORT: 3006
    },
    error_file: './logs/err.log',
    out_file: './logs/out.log',
    log_file: './logs/combined.log',
    time: true
  }]
}
```

**PM2常用命令：**
```bash
# 启动应用
pm2 start ecosystem.config.js

# 停止应用
pm2 stop art-design-pro

# 重启应用
pm2 restart art-design-pro

# 查看状态
pm2 status

# 查看日志
pm2 logs art-design-pro

# 设置开机自启动
pm2 startup
pm2 save

# 删除应用
pm2 delete art-design-pro
```

#### 方法四：Docker容器自动启动

**创建Dockerfile：**
```dockerfile
FROM node:18-alpine

WORKDIR /app

# 安装pnpm
RUN npm install -g pnpm

# 复制项目文件
COPY admin/package*.json ./
COPY admin/pnpm-lock.yaml ./

# 安装依赖
RUN pnpm install

# 复制源代码
COPY admin/ ./

# 暴露端口
EXPOSE 3006

# 启动命令
CMD ["pnpm", "dev", "--host", "0.0.0.0"]
```

**Docker Compose配置 `docker-compose.yml`：**
```yaml
version: '3.8'
services:
  art-design-pro:
    build: .
    ports:
      - "3006:3006"
    volumes:
      - ./admin:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped
    container_name: art-design-pro-backend
```

**Docker启动命令：**
```bash
# 构建并启动
docker-compose up -d

# 查看状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

### ⚙️ 自动启动配置建议

#### 🎯 推荐配置

1. **开发环境**：使用PM2管理进程
2. **生产环境**：使用Docker + PM2
3. **个人使用**：Windows任务计划程序
4. **团队协作**：Docker Compose

#### 📋 自动启动检查清单

启动前确认：
- ✅ Node.js环境正常
- ✅ pnpm包管理器可用
- ✅ 项目依赖已安装
- ✅ 端口3006未被占用
- ✅ 环境配置文件存在
- ✅ 网络连接正常

#### 🔧 自动启动故障处理

**常见问题：**

1. **服务启动失败**
   ```bash
   # 检查端口占用
   netstat -ano | findstr :3006
   
   # 强制终止占用进程
   taskkill /PID 进程ID /F
   ```

2. **依赖缺失**
   ```bash
   # 重新安装依赖
   cd admin
   rm -rf node_modules
   pnpm install
   ```

3. **权限问题**
   ```bash
   # 以管理员身份运行PowerShell
   # 然后执行启动命令
   ```

4. **路径错误**
   ```bash
   # 检查项目路径是否正确
   echo $PWD  # Linux/Mac
   echo %CD%  # Windows
   ```

## 📈 性能优化

### 启动速度对比
- **快速启动**: ~2-3秒
- **完整启动**: ~5-10秒（首次）
- **菜单模式**: 按需选择，灵活高效

### 内存占用
- **内部版本**: 轻量级，适合开发环境
- **外部版本**: 功能完整，适合独立使用

## 🎉 特色功能

### 🌈 彩色界面
- 不同功能使用不同颜色主题
- 成功/警告/错误信息清晰区分

### 🔄 智能检查
- 自动检测环境配置
- 提供具体的解决建议

### 📊 详细反馈
- 实时显示操作进度
- 完整的错误信息和建议

### 🎛️ 灵活配置
- 支持多种启动方式
- 适应不同使用场景

---

## 📞 技术支持

如果遇到问题：
1. 首次使用请阅读本说明文档
2. 运行"检查配置依赖"诊断问题
3. 查看控制台详细错误信息
4. 参考故障排除章节

## 📋 **快速参考**

### 🔥 **常用命令（内部版本）**
```bash
# 日常开发
./网站工具/启动服务器/内部/快速启动后台.bat    # 后台管理
./网站工具/启动服务器/内部/快速启动后端.bat    # 后端API

# 菜单启动
./网站工具/启动服务器/内部/菜单.bat          # 主菜单

# 停止服务
./网站工具/启动服务器/内部/其他启动/停止服务器.bat
```

### 🌐 **访问地址**
- **后台管理**: http://localhost:3006/
- **后端API**: http://localhost:3001/api/
- **健康检查**: http://localhost:3001/api/health

---

**🎊 祝您使用愉快！Art Design Pro 全栈开发启动工具让开发更高效！** 🚀 